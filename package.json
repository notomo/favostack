{
  "private": true,
  "name": "favostack",
  "type": "module",
  "version": "0.1.6",
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.0.6",
    "wxt": "^0.20.11"
  },
  "scripts": {
    "dev": "wxt",
    "build": "wxt zip",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "postinstall": "wxt prepare",
    "submit": "wxt submit --chrome-zip .output/*-chrome.zip",
    "submit:dryrun": "wxt submit --dry-run --chrome-zip .output/*-chrome.zip"
  }
}
